# Admin Server

Placeholder. Future Go backend for analytics, licenses, and purchases. Arrives as a nested Go module (`github.com/hay-kot/hive-desktop/server`) with its own deploy workflow — it is not part of the root module. Design is a separate doc before any code lands.
