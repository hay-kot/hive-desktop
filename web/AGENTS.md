# Agent Instructions — Landing page and docs

Scope: `web/`, the public site at `hivedesktop.com`. The repository-root
`AGENTS.md` still applies (git standards, quality gates).

An Astro static build (`web/src/`) output to `web/dist/` and served as
Cloudflare Workers static assets (`web/wrangler.jsonc`; the custom domain is
declared there and attaches on deploy).

Deploys via `.github/workflows/deploy-web.yml` on pushes to main touching
`web/**`, or locally with `npm run deploy`.

## Page copy is data, not markup

The JSON in `web/src/data/` is schema-validated in `web/src/data/index.ts`, so a
bad edit fails the build.

Nav and footer links with `"href": null` in `web/src/data/site.json` are not
rendered; filling in the URL is all it takes to bring one back.

## Worker routes

`web/worker/index.ts` adds two routes on top of the assets:

- `/api/latest` — proxies the release manifest for the download CTA.
- `/api/report` — gzipped diagnostic bundles from the app's problem reporter,
  written to the private `hive-desktop-reports` R2 bucket (ADR
  in-app-problem-reporting).

## Docs

Docs live in the `docs` content collection (`web/src/content/docs/`), served at
`/docs` by `web/src/pages/docs/[...slug].astro` through the `Docs` layout.
Adding a page is a content change: drop in a Markdown file with
`title`/`description`/`group`/`order` frontmatter and the sidebar, pager, and
on-this-page TOC pick it up.

Search is Pagefind, regenerated by the `pagefind` step in `npm run build` and
scoped to docs via `data-pagefind-body`. The docs pages opt out of the marketing
backdrop (`Base`'s `backdrop={false}`) for readability.

The same collection is served for LLMs: `/llms.txt` (one link per page),
`/llms-full.txt` (every page inlined), and `/docs/<id>.md` (one page as
Markdown), all from `src/pages/` at build time. Callouts are GitHub-style
`> [!TIP]` blockquotes, rendered by `src/lib/remark-callouts.mjs`.

Two repo skills govern this site: `web-docs` (the mechanics of a page) and
`docs-audit` (whether a change on a branch needs one, and where).

This is user-facing product documentation only — `docs/architecture.md`, ADRs,
and in-app copy are not this site.
