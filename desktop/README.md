# Desktop App

Placeholder. The Wails v3 application (`desktop/` + `internal/desktop/` trees) is imported here from `colonyops/hive` in extraction Phase 2 as a single `Import desktop from colonyops/hive@<sha>` commit. Do not add code here before that import lands.
