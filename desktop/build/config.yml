# This file contains the configuration for this project.
# When you update `info`, run `wails3 task common:update:build-assets` to update the assets.
# Note that this will overwrite any changes you have made to the assets. macOS uses
# the committed build/darwin/icons.icns generated by build/icons/generate.sh.
version: '3'

# This information is used to generate the build assets.
info:
  companyName: "ColonyOps"
  productName: "Hive"
  productIdentifier: "com.hivedesktop.app"
  description: "Hive desktop application"
  copyright: "(c) 2026 ColonyOps"
  comments: ""
  version: "0.1.0"

# root_path is the repo root so the watcher sees internal/** and cmd/**;
# executes set `dir: desktop` to run the build from the app package.
dev_mode:
  root_path: ..
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - node_modules
      - frontend
      - bin
    file:
      - .DS_Store
      - .gitignore
      - .gitkeep
      - "*_test.go"
    watched_extension:
      - "*.go"
      - "*.js" # Watch for changes to JS/TS files included using the //wails:include directive.
      - "*.ts" # The frontend directory will be excluded entirely by the setting above.
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      dir: desktop
      type: blocking
    - cmd: wails3 task common:dev:frontend
      dir: desktop
      type: background
    - cmd: wails3 task run
      dir: desktop
      type: primary
