---
version: 0.1.8-dev.25
date: 2026-08-06
summary: A new sources.exec connector, the agent surface moves to MCP tools, and terminal lifecycle fixes.
---

- sources.exec: run a command on the poll tick and ingest its stdout as a snapshot
- Serve the agent surface as MCP tools, replacing the REST API
- Present a dead terminal session as not started, not as an error
- Identify ADRs by date and slug so concurrent branches cannot collide
- Create agent chats through a dialog instead of a full-page panel
- Bind the terminal window lifecycle to the chords terminal emulators use
- Move wails v3 to the beta line, and give the e2e suite a gate again
- Validate MCP catalogue commands against the resolved PATH
