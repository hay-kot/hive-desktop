---
kind: changed
---

**⌘+ and ⌘- now step the terminal text size** instead of zooming the window, so the grid reflows and rewraps rather than growing a horizontal scrollbar. ⌘0 puts the size back, the change persists like the Settings ▸ Terminal one it writes, and window zoom stays available in the View menu.
