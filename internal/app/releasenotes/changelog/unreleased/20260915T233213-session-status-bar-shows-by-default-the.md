---
kind: changed
---

**Session status bar shows by default**: the attached session's directory, branch, and pull request now appear above the terminal. Existing settings are updated once on the next launch. Turn it off in Settings ▸ Terminal.
