---
version: 0.1.8-dev.12
date: 2026-07-30
summary: The first published dev build of the 0.1.8 line.
---

- Switch desktop auto-update and publishing to R2 channel manifests
- Inbox trash redesign and feed row hover actions
- Add Renovate config for automated dependency updates
- Enforce quality gates with lefthook git hooks
- Restore default ignorePaths and use cron schedule syntax
- Dev mode: run against an ephemeral copy of local data and config
- Persist theme in settings.yaml instead of webview localStorage
- Add generic webhook source and make the inbox item contract provider-neutral
- Centralize LLM prompts in Go and add an LLM prompts settings page
- Add a notify terminal node to the flow editor
- Let a feed notify on new items
- feat: folder edit dialog
- feat: feed date groups
- Roll out the themed select and remove native <select> elements
- Reorder actions by drag and drop
- Feed: mark all as read
- Harden desktop release publishing
- Strip inline markdown from node help summaries
- rework entire app
- Add cmd/devserver: shared dev GitHub proxy and event simulator
- Add deadcode and govulncheck gates; enable three linters
- Batch GitHub absence confirmation to fix the rate-limit N+1
- Rebuild landing page as Astro site
- Add agent-facing devserver control API (runtime scenarios, inline targets, discovery)
- Ship a richer default action catalog
- Add pprof debug endpoint on its own loopback listener
- Notify-node-only notifications + durable function-node KV
- Add in-app problem reporting to a private R2 bucket
- Profile avatar images + agent HTTP API for profiles
- Stamp report token and deploy+verify web during release
- Serve the install script behind the obscure invite token (#56)
- Ask for notification permission during first-run onboarding
- Adapt settings pages to narrow widths and split-screen
- Self-describing agent HTTP API: route index + generated OpenAPI
- Publish Linux releases and close the Linux runtime gaps
- Add a native clipboard action type
- fix: stop pre-#63 empty-scope inbox rows from wedging the flow consumer
- Create a Hive session from a form (from an item or standalone) (#81)
- Let a webhook source use an uploaded image for its feed mark (#91)
- Pin dependencies
- Redesign Activity view as a ledger and condense the item actions menu
- Add forward-only YAML config migration system
- feat: add Grafana source connector (metrics + alerts)
- Install config prompts as agent skills
- Docs site on the landing page, with search
- chore(grafana): trim connector comments to house density
- Publish GitHub tags and Releases during desktop releases
- Split one source message into per-entity feed items
- Terminal mode: tmux-backed sessions behind experimental.terminal
- Replace the app icon with the four-node hive mark
