// Code generated by go-enum DO NOT EDIT.
// Version: v0.9.4

// Built By: go install

package activity

import (
	"fmt"
	"strings"
)

const (
	// CategoryRefresh is a Category of type refresh.
	CategoryRefresh Category = "refresh"
	// CategorySession is a Category of type session.
	CategorySession Category = "session"
	// CategoryAutoAction is a Category of type auto_action.
	CategoryAutoAction Category = "auto_action"
	// CategoryAction is a Category of type action.
	CategoryAction Category = "action"
	// CategoryConfig is a Category of type config.
	CategoryConfig Category = "config"
	// CategorySystem is a Category of type system.
	CategorySystem Category = "system"
)

var ErrInvalidCategory = fmt.Errorf("not a valid Category, try [%s]", strings.Join(_CategoryNames, ", "))

var _CategoryNames = []string{
	string(CategoryRefresh),
	string(CategorySession),
	string(CategoryAutoAction),
	string(CategoryAction),
	string(CategoryConfig),
	string(CategorySystem),
}

// CategoryNames returns a list of possible string values of Category.
func CategoryNames() []string {
	tmp := make([]string, len(_CategoryNames))
	copy(tmp, _CategoryNames)
	return tmp
}

// String implements the Stringer interface.
func (x Category) String() string {
	return string(x)
}

// IsValid provides a quick way to determine if the typed value is
// part of the allowed enumerated values
func (x Category) IsValid() bool {
	_, err := ParseCategory(string(x))
	return err == nil
}

var _CategoryValue = map[string]Category{
	"refresh":     CategoryRefresh,
	"session":     CategorySession,
	"auto_action": CategoryAutoAction,
	"action":      CategoryAction,
	"config":      CategoryConfig,
	"system":      CategorySystem,
}

// ParseCategory attempts to convert a string to a Category.
func ParseCategory(name string) (Category, error) {
	if x, ok := _CategoryValue[name]; ok {
		return x, nil
	}
	// Case insensitive parse, do a separate lookup to prevent unnecessary cost of lowercasing a string if we don't need to.
	if x, ok := _CategoryValue[strings.ToLower(name)]; ok {
		return x, nil
	}
	return Category(""), fmt.Errorf("%s is %w", name, ErrInvalidCategory)
}

// MarshalText implements the text marshaller method.
func (x Category) MarshalText() ([]byte, error) {
	return []byte(string(x)), nil
}

// UnmarshalText implements the text unmarshaller method.
func (x *Category) UnmarshalText(text []byte) error {
	tmp, err := ParseCategory(string(text))
	if err != nil {
		return err
	}
	*x = tmp
	return nil
}

// AppendText appends the textual representation of itself to the end of b
// (allocating a larger slice if necessary) and returns the updated slice.
//
// Implementations must not retain b, nor mutate any bytes within b[:len(b)].
func (x *Category) AppendText(b []byte) ([]byte, error) {
	return append(b, x.String()...), nil
}

const (
	// SeverityInfo is a Severity of type info.
	SeverityInfo Severity = "info"
	// SeveritySuccess is a Severity of type success.
	SeveritySuccess Severity = "success"
	// SeverityWarning is a Severity of type warning.
	SeverityWarning Severity = "warning"
	// SeverityError is a Severity of type error.
	SeverityError Severity = "error"
	// SeverityAuto is a Severity of type auto.
	SeverityAuto Severity = "auto"
)

var ErrInvalidSeverity = fmt.Errorf("not a valid Severity, try [%s]", strings.Join(_SeverityNames, ", "))

var _SeverityNames = []string{
	string(SeverityInfo),
	string(SeveritySuccess),
	string(SeverityWarning),
	string(SeverityError),
	string(SeverityAuto),
}

// SeverityNames returns a list of possible string values of Severity.
func SeverityNames() []string {
	tmp := make([]string, len(_SeverityNames))
	copy(tmp, _SeverityNames)
	return tmp
}

// String implements the Stringer interface.
func (x Severity) String() string {
	return string(x)
}

// IsValid provides a quick way to determine if the typed value is
// part of the allowed enumerated values
func (x Severity) IsValid() bool {
	_, err := ParseSeverity(string(x))
	return err == nil
}

var _SeverityValue = map[string]Severity{
	"info":    SeverityInfo,
	"success": SeveritySuccess,
	"warning": SeverityWarning,
	"error":   SeverityError,
	"auto":    SeverityAuto,
}

// ParseSeverity attempts to convert a string to a Severity.
func ParseSeverity(name string) (Severity, error) {
	if x, ok := _SeverityValue[name]; ok {
		return x, nil
	}
	// Case insensitive parse, do a separate lookup to prevent unnecessary cost of lowercasing a string if we don't need to.
	if x, ok := _SeverityValue[strings.ToLower(name)]; ok {
		return x, nil
	}
	return Severity(""), fmt.Errorf("%s is %w", name, ErrInvalidSeverity)
}

// MarshalText implements the text marshaller method.
func (x Severity) MarshalText() ([]byte, error) {
	return []byte(string(x)), nil
}

// UnmarshalText implements the text unmarshaller method.
func (x *Severity) UnmarshalText(text []byte) error {
	tmp, err := ParseSeverity(string(text))
	if err != nil {
		return err
	}
	*x = tmp
	return nil
}

// AppendText appends the textual representation of itself to the end of b
// (allocating a larger slice if necessary) and returns the updated slice.
//
// Implementations must not retain b, nor mutate any bytes within b[:len(b)].
func (x *Severity) AppendText(b []byte) ([]byte, error) {
	return append(b, x.String()...), nil
}
